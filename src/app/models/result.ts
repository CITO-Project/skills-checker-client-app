export interface Result {
  literacy: {
    level: number;
    priority: string;
  };
  numeracy: {
    level: number;
    priority: string;
  };
  digital_skills: {
    level: number;
    priority: string;
  };
}
